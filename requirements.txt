# Use Python 3.11 for best wheel compatibility on Windows.
# These pins prefer versions that are known to have prebuilt wheels for CPython 3.11
# If you use conda, prefer using conda-forge channel instead of pip for binaries (recommended).

pandas==2.1.3
numpy==1.26.4
matplotlib==3.7.2
seaborn==0.12.2
scikit-learn==1.2.2
statsmodels==0.14.5
pmdarima==2.1.1
pytest==7.4.0
openpyxl==3.1.2
